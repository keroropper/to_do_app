<!DOCTYPE html>
<html>
  <head>
    <title>Myapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://unpkg.com/ress/dist/ress.min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <div class="container-fluid">
      <div class="row">
          <%= render 'shared/header' unless request.path.include?("login") || request.path.include?("signup") %>
          <div class="<%= request.path.include?('login') || request.path.include?('signup') ? '' : 'col-md-7 main-contents' %>">
            <%= yield %>
          </div>
          <%= render 'shared/side_bar' unless request.path.include?("login") || request.path.include?("signup") %>
      </div>
    </div>
  </body>
</html>
