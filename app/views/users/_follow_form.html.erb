<% unless current_user && current_user != @user %>
  <div id="follow-form">
    <% if current_user.followed_by?(@user) %>
      <%= render 'unfollow' %>
    <% else %>
      <%= render 'follow' %>
    <% end %>
  </div>
<% end %>