
  <h1><%= @title %></h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <% if user.image.attached? %>
        <%= image_tag user.profile(100), class: "image" %>
      <% end %>
      <%= link_to user.name, user_path(user) %>
    </li>
  <% end %>
</ul>